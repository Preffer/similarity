doctype html
html
	head
		meta(charset='UTF-8')
		meta(http-equiv='X-UA-Compatible' content='IE=edge, chrome=1')
		meta(name='description' content='Similarity')
		meta(name='renderer' content='webkit')
		meta(name='viewport' content='width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no')
		block Similarity
		link(rel='stylesheet', href='/components/bootstrap/dist/css/bootstrap.min.css')
		link(rel='stylesheet', href='/components/seiyria-bootstrap-slider/dist/css/bootstrap-slider.min.css')
		link(rel='stylesheet', href='/css/style.css')
		link(rel='icon' href='/img/favicon.ico')

	body
		nav.navbar.navbar-inverse.navbar-fixed-top
			.container
				.navbar-header
					a.navbar-brand Similarity

		.container
			.jumbotron
				h2 DCT based watermark
				ul
					li Encode low frequency signal of watermark image into high frequency portion of color image
					li If the image size dis-match, the watermark image will be stretched to match the color image
					li You must set the following parameters to be same between watermarking and detection
					li <strong>Slice:</strong> The portion of color image used to carry watermark image
					li <strong>Shift:</strong> Bits shifted before encode watermark signal into color signal

			.row
				.col-sm-6.float-none.center-block
					.thumbnail#target.dropable
						img.pointer
						.caption
							input.center-block(type='file' name='color')
							h3.text-center Click to upload your target image

			.row
				.col-sm-2.float-none.center-block
					button.btn.btn-success#add-image
						i.glyphicon.glyphicon-plus
						span &nbsp;Add image
					input.hidden#add-file(type='file' multiple)

			.list-group#case

		footer.text-center
			p Design and code by &nbsp;
				a(href='mailto:PillowSky.qq.com') PillowSky@qq.com
			p Powered by Node.js / Express / Jimp / Async

		script(type='text/x-tmpl' id='tmpl-row')
			.list-group-item.row(id='{%=o.id%}')
				.col-xs-2
					img.img-responsive(src='{%=o.src%}')
				.col-xs-4
					.progress
						.progress-bar(role='progressbar' style='width: 0%')
				.col-xs-2
					p.hash {%=o.hash%}
				.col-xs-2
					p.distance {%=o.distance%}

		script(src='/components/jquery/dist/jquery.min.js')
		script(src='/components/bootstrap/dist/js/bootstrap.min.js')
		script(src='/components/seiyria-bootstrap-slider/dist/bootstrap-slider.min.js')
		script(src='/components/blueimp-tmpl/js/tmpl.min.js')
		script(src='/js/script.js')
